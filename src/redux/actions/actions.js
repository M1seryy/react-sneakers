export const SET_TO_BASKET = (type, payload) => {
  return {
    type,
    payload,
  };
};
export const DELETE_ITEM = (type, payload) => {
  return {
    type,
    payload,
  };
};

export const SET_BASKET = (type, payload) => {
  return {
    type,
    payload,
  };
};
